# 📁 TestHub IoT - 完整项目结构

## 🌲 项目目录树

```
TestHub/
│
├── 📄 README.md                       # 项目说明（架构图、技术栈、快速开始）
├── 📄 QUICK_START.md                  # 快速启动指南（详细步骤）
├── 📄 PROJECT_SUMMARY.md              # 项目总结（核心代码理解、面试准备）
├── 📄 DELIVERY_CHECKLIST.md           # 交付清单（完整的交付物清单）
├── 📄 CAREER_ADVICE.md                # 职业建议（职业规划、学习路线）
├── 📄 交付总结.md                      # 最终交付总结
│
├── ⚙️ pom.xml                         # Maven父工程配置
├── 🐳 docker-compose.yml              # Docker编排文件（一键部署）
│
├── 📂 sql/
│   └── 🗄️ testhub-init.sql           # 数据库初始化脚本（8张表+测试数据）
│
├── 📦 testhub-common/                 # 公共模块
│   ├── pom.xml
│   └── src/main/java/com/testhub/common/
│       ├── dto/
│       │   └── Result.java            # ✅ 统一响应类（150行）
│       ├── entity/
│       │   └── BaseEntity.java        # ✅ 基础实体类（50行）
│       ├── utils/
│       │   └── JwtUtils.java          # ✅ JWT工具类（200行）
│       ├── exception/
│       │   └── BusinessException.java # ✅ 业务异常类（60行）
│       └── constants/
│           └── Constants.java         # ✅ 系统常量（150行）
│
├── 🌐 testhub-gateway/                # API网关服务 (8080)
│   ├── pom.xml
│   ├── src/main/java/com/testhub/gateway/
│   │   └── GatewayApplication.java    # ✅ 网关启动类
│   └── src/main/resources/
│       └── application.yml            # ✅ 网关配置（路由、跨域）
│
├── 🔐 testhub-auth/                   # 认证服务 (8081)
│   ├── pom.xml
│   ├── src/main/java/com/testhub/auth/
│   │   ├── AuthApplication.java       # ✅ 认证服务启动类
│   │   ├── controller/                # ⚠️ 待实现
│   │   ├── service/                   # ⚠️ 待实现
│   │   ├── mapper/                    # ⚠️ 待实现
│   │   └── entity/                    # ⚠️ 待实现
│   └── src/main/resources/
│       └── application.yml            # ✅ 认证服务配置
│
├── 📊 testhub-project/                # 项目管理服务 (8082)
│   ├── pom.xml                        # ✅ 项目服务依赖
│   ├── src/main/java/com/testhub/project/
│   │   ├── controller/                # ⚠️ 待实现
│   │   ├── service/                   # ⚠️ 待实现
│   │   ├── mapper/                    # ⚠️ 待实现
│   │   └── entity/                    # ⚠️ 待实现
│   └── src/main/resources/
│
└── 🧪 testhub-testcase/               # 测试用例服务 (8083)
    ├── pom.xml                        # ✅ 测试服务依赖（含MQTT、WebSocket、EasyExcel）
    ├── src/main/java/com/testhub/testcase/
    │   ├── controller/                # ⚠️ 待实现
    │   ├── service/                   # ⚠️ 待实现
    │   ├── mapper/                    # ⚠️ 待实现
    │   ├── entity/                    # ⚠️ 待实现
    │   └── mqtt/                      # ⚠️ 待实现（MQTT客户端）
    └── src/main/resources/
```

---

## 📊 文件统计

### 文档文件（6份）

| 文件名 | 字数 | 用途 |
|--------|------|------|
| README.md | ~1500字 | 项目介绍、快速开始 |
| QUICK_START.md | ~1200字 | 详细启动步骤、测试指南 |
| PROJECT_SUMMARY.md | ~2000字 | 核心代码理解、面试准备 |
| DELIVERY_CHECKLIST.md | ~1000字 | 交付清单、验收标准 |
| CAREER_ADVICE.md | ~1800字 | 职业规划、学习路线 |
| 交付总结.md | ~1200字 | 最终交付总结 |
| **合计** | **~8700字** | **完整文档体系** |

### 代码文件（21个）

| 模块 | 文件数 | 代码行数 | 完成度 |
|------|--------|----------|--------|
| 公共模块 | 6 | ~690行 | 100% |
| 网关服务 | 3 | ~250行 | 100% |
| 认证服务 | 3 | ~180行 | 骨架100% |
| 项目服务 | 1 | ~80行 | 骨架100% |
| 测试服务 | 1 | ~100行 | 骨架100% |
| 数据库脚本 | 1 | ~350行 | 100% |
| Docker配置 | 1 | ~150行 | 100% |
| POM配置 | 5 | ~500行 | 100% |
| **合计** | **21** | **~2300行** | **核心90%** |

---

## 🎯 核心文件详解

### 1. 公共模块核心文件

#### Result.java（统一响应类）
**路径**：`testhub-common/src/main/java/com/testhub/common/dto/Result.java`
**作用**：所有API接口返回统一格式
**核心方法**：
```java
Result.success()         // 成功响应（无数据）
Result.success(data)     // 成功响应（带数据）
Result.fail(message)     // 失败响应
Result.unauthorized()    // 未认证
Result.forbidden()       // 无权限
```

#### JwtUtils.java（JWT工具类）
**路径**：`testhub-common/src/main/java/com/testhub/common/utils/JwtUtils.java`
**作用**：生成和解析JWT Token
**核心方法**：
```java
JwtUtils.generateToken(claims)          // 生成Token
JwtUtils.parseToken(token)               // 解析Token
JwtUtils.validateToken(token)            // 验证Token
JwtUtils.getUserIdFromToken(token)       // 获取用户ID
JwtUtils.isTokenExpiringSoon(token)      // 检查Token是否即将过期
```

#### Constants.java（系统常量）
**路径**：`testhub-common/src/main/java/com/testhub/common/constants/Constants.java`
**作用**：定义系统常量，避免魔法数字
**包含**：
- JWT相关常量（密钥、有效期、请求头）
- Redis Key前缀
- 角色权限常量
- 设备类型常量
- 测试用例类型常量
- MQTT主题常量

---

### 2. 网关核心配置

#### application.yml（网关配置）
**路径**：`testhub-gateway/src/main/resources/application.yml`
**作用**：配置路由规则、跨域、服务发现
**核心配置**：

```yaml
# 路由规则示例
routes:
  - id: testhub-auth
    uri: lb://testhub-auth      # 负载均衡，从Nacos获取服务地址
    predicates:
      - Path=/api/auth/**        # 匹配路径
    filters:
      - StripPrefix=1            # 去除/api前缀
```

**请求流程：**
```
客户端请求: http://localhost:8080/api/auth/login
  ↓
网关匹配路由: testhub-auth
  ↓
去除前缀: /auth/login
  ↓
从Nacos查询服务地址: http://localhost:8081
  ↓
转发到: http://localhost:8081/auth/login
```

---

### 3. 数据库核心表

#### tb_user（用户表）
**用途**：存储用户信息和角色
**关键字段**：
- username：用户名（唯一）
- password：密码（BCrypt加密）
- roles：角色列表（逗号分隔，如"ADMIN,TESTER"）
- status：状态（0-禁用 1-正常）

#### tb_device（IoT设备表）
**用途**：存储IoT设备信息
**关键字段**：
- device_name：设备名称
- device_type：设备类型（ROBOT_VACUUM、AIR_PURIFIER等）
- mqtt_client_id：MQTT客户端ID（唯一）
- status：设备状态（ONLINE、OFFLINE、TESTING）

#### tb_testcase（测试用例表）
**用途**：存储测试用例
**关键字段**：
- case_no：用例编号（唯一）
- test_type：测试类型（SMOKE、FUNCTIONAL、PERFORMANCE）
- priority：优先级（P0-P3）
- test_steps：测试步骤（JSON格式）

**test_steps示例：**
```json
[
  {"step":1,"action":"按下电源键","expected":"听到开机提示音"},
  {"step":2,"action":"观察设备状态","expected":"LED灯亮起"},
  {"step":3,"action":"检查MQTT连接","expected":"设备上线"}
]
```

---

## 🔍 代码阅读顺序

### 第一阶段：理解基础组件（2-3小时）

1. **Result.java**（30分钟）
   - 理解统一响应格式
   - 掌握静态工厂方法使用
   - 了解泛型的应用

2. **BaseEntity.java**（15分钟）
   - 理解公共字段的抽取
   - 掌握MyBatis Plus自动填充
   - 了解逻辑删除机制

3. **JwtUtils.java**（1小时）
   - 理解JWT生成和解析
   - 掌握Token验证流程
   - 了解异常处理机制

4. **Constants.java**（30分钟）
   - 熟悉系统常量定义
   - 理解常量的使用场景

5. **BusinessException.java**（15分钟）
   - 理解业务异常封装
   - 掌握异常的使用方式

---

### 第二阶段：理解微服务架构（1-2小时）

1. **gateway/application.yml**（1小时）
   - 理解路由配置
   - 掌握服务发现机制
   - 了解跨域配置

2. **GatewayApplication.java**（15分钟）
   - 理解网关启动流程
   - 了解@EnableDiscoveryClient注解

3. **auth/application.yml**（30分钟）
   - 理解数据源配置
   - 掌握Nacos配置
   - 了解MyBatis Plus配置

---

### 第三阶段：理解数据库设计（1小时）

1. **testhub-init.sql**（1小时）
   - 理解表结构设计
   - 掌握外键关系
   - 了解测试数据

---

### 第四阶段：理解部署方案（30分钟）

1. **docker-compose.yml**（30分钟）
   - 理解容器编排
   - 掌握服务依赖
   - 了解数据持久化

---

## 🛠️ 如何扩展新功能

### 示例：添加"测试报告生成"功能

#### 步骤1：定义实体类
```java
// testhub-testcase/src/main/java/com/testhub/testcase/entity/TestReport.java
@Data
@TableName("tb_test_report")
public class TestReport extends BaseEntity {
    private String reportNo;
    private Long projectId;
    private String reportName;
    private Integer totalCases;
    private Integer passedCases;
    private Integer failedCases;
    private BigDecimal passRate;
}
```

#### 步骤2：创建Mapper
```java
// testhub-testcase/src/main/java/com/testhub/testcase/mapper/TestReportMapper.java
@Mapper
public interface TestReportMapper extends BaseMapper<TestReport> {
}
```

#### 步骤3：创建Service
```java
// testhub-testcase/src/main/java/com/testhub/testcase/service/TestReportService.java
@Service
public class TestReportService {
    @Autowired
    private TestReportMapper reportMapper;

    public TestReport generateReport(Long projectId) {
        // 统计测试用例执行结果
        // 计算通过率
        // 生成报告
        // 保存到数据库
        return report;
    }
}
```

#### 步骤4：创建Controller
```java
// testhub-testcase/src/main/java/com/testhub/testcase/controller/TestReportController.java
@RestController
@RequestMapping("/report")
public class TestReportController {

    @Autowired
    private TestReportService reportService;

    @PostMapping("/generate")
    public Result<TestReport> generate(@RequestParam Long projectId) {
        TestReport report = reportService.generateReport(projectId);
        return Result.success(report);
    }
}
```

#### 步骤5：测试接口
```bash
curl -X POST http://localhost:8080/api/testcase/report/generate?projectId=1 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 📝 开发规范

### 命名规范

**包命名**：全小写，单词之间用点分隔
```
com.testhub.testcase.controller
```

**类命名**：大驼峰
```
TestCaseController、TestReportService
```

**方法命名**：小驼峰
```
generateTestReport()、listByProjectId()
```

**常量命名**：全大写，下划线分隔
```
REDIS_LOGIN_TOKEN_PREFIX
```

### 注释规范

**类注释**：说明类的用途
```java
/**
 * 测试报告服务
 *
 * 作用：生成测试报告、统计测试结果
 *
 * @author TestHub Team
 */
public class TestReportService {
}
```

**方法注释**：说明输入、输出、用途
```java
/**
 * 生成测试报告
 *
 * @param projectId 项目ID
 * @return 测试报告对象
 */
public TestReport generateReport(Long projectId) {
}
```

### 异常处理规范

**业务异常**：抛出BusinessException
```java
if (project == null) {
    throw new BusinessException("项目不存在");
}
```

**系统异常**：记录日志并抛出
```java
try {
    // 业务逻辑
} catch (Exception e) {
    log.error("生成报告失败", e);
    throw new BusinessException(500, "生成报告失败");
}
```

---

## 🎯 总结

这个项目结构清晰、模块划分合理、代码规范统一。

**核心优势：**
1. ✅ 公共模块封装完整（Result、JWT、异常、常量）
2. ✅ 微服务架构设计合理（网关、认证、业务分离）
3. ✅ 数据库设计完善（8张表，支持完整业务流程）
4. ✅ 部署方案完整（Docker一键部署）
5. ✅ 文档齐全（从入门到精通）

**扩展性：**
- 新增微服务只需添加模块和路由配置
- 新增功能只需添加Controller/Service/Mapper
- 新增表只需继承BaseEntity

**建议：**
1. 先理解现有代码（2-3天）
2. 完善业务逻辑（1-2周）
3. 编写单元测试（1周）
4. 准备面试话术（3-5天）

**下一步行动：**
查看QUICK_START.md启动项目！

---

**项目位置**：`/home/sutai/TestHub`
**完整文档**：查看根目录下的Markdown文件
**交付日期**：2026-01-14
**版本号**：v1.0.0-MVP
